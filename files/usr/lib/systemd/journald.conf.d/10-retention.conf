[Journal]
# The image build strips /var/log and systemd's tmpfiles only adjust /var/log/journal, they
# never create it. "persistent" makes journald create the directory itself, so the retention
# below is guaranteed to govern an on-disk journal rather than a volatile one.
Storage=persistent
MaxRetentionSec=7d
